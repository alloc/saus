{
  "name": "@saus/cloudform",
  "version": "0.1.0",
  "description": "Manage your AWS infra with CloudFormation",
  "license": "MIT",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup --clean",
    "dev": "tsup --watch --sourcemap",
    "prepare": "yarn build"
  },
  "peerDependencies": {
    "saus": "*"
  },
  "devDependencies": {
    "recrawl-sync": "^2.2.1",
    "saus": "*"
  },
  "dependencies": {
    "cloudform-types": "^7.3.0",
    "type-fest": "^2.13.0"
  }
}
