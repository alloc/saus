{
  "name": "@saus/cloudform",
  "version": "0.1.0",
  "description": "Manage your AWS infra with CloudFormation",
  "license": "MIT",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "index.d.ts",
  "files": [
    "dist",
    "index.d.ts"
  ],
  "scripts": {
    "build": "tsup --clean",
    "dev": "tsup --watch --sourcemap",
    "prepare": "yarn build"
  },
  "peerDependencies": {
    "saus": "*"
  },
  "devDependencies": {
    "recrawl-sync": "^2.2.1",
    "saus": "*"
  },
  "dependencies": {
    "@saus/aws-utils": "*",
    "cloudform-types": "npm:@alloc/cloudform-types@^7.4.3",
    "type-fest": "^2.13.0"
  }
}
