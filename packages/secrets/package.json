{
  "name": "@saus/secrets",
  "version": "0.1.0",
  "description": "Load password-encrypted secrets from git",
  "license": "MIT",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "index.d.ts",
  "files": [
    "dist",
    "index.d.ts"
  ],
  "scripts": {
    "build": "tsup --clean",
    "dev": "tsup --watch --sourcemap",
    "prepare": "yarn build"
  },
  "peerDependencies": {
    "saus": "*"
  },
  "devDependencies": {
    "recrawl-sync": "^2.2.1",
    "saus": "link:../.."
  },
  "dependencies": {
    "@saus/deploy-utils": "*",
    "aes-password": "^1.0.5"
  }
}
