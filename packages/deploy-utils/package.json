{
  "name": "@saus/deploy-utils",
  "version": "0.1.0",
  "description": "Common logic for deployment plugins",
  "license": "MIT",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup --clean",
    "dev": "tsup --watch --sourcemap",
    "prepare": "yarn build"
  },
  "dependencies": {
    "@types/prompts": "^2.0.14",
    "prompts": "^2.4.2"
  },
  "devDependencies": {
    "recrawl-sync": "^2.2.1"
  }
}
