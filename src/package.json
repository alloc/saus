{
  "private": true,
  "scripts": {
    "build": "rimraf ../dist && tsup",
    "dev": "rimraf ../dist && tsup --watch --sourcemap",
    "lint": "tsc --build && eslint . --ext .ts"
  },
  "dependencies": {
    "@": "link:./core",
    "@runtime": "workspace:@saus/runtime@*",
    "@utils": "workspace:@saus/utils@*",
    "@vm": "workspace:@saus/vm@*"
  }
}
