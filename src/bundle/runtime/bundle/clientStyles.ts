/**
 * The CSS required by each route client.
 */
const clientStyles: Record<string, string[]> = (globalThis as any)
  .sausClientStyles

// Stub module replaced at build time.
export default clientStyles
