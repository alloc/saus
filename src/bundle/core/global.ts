// Redirect "saus/src/core/global" imports here.
import { noop } from '../../utils/noop'
import { context } from '../context'
export { context as routesModule, context as renderModule }
export const setRoutesModule = noop
