{
  "private": true,
  "scripts": {
    "clean": "rimraf dist && git checkout head -- dist",
    "build": "npm run clean && tsc --incremental -p . && esbuild **/*.ts --out-extension:.js=.mjs --outdir=dist",
    "dev": "npm run clean && tsc --incremental -p . --watch --sourcemap"
  },
  "dependencies": {
    "ohash": "^0.1.5",
    "tinypool": "^0.1.1"
  }
}
