{
  "extends": "../../tsconfig.json",
  "include": ["**/*.ts"],
  "exclude": ["**/*.spec.ts"],
  "compilerOptions": {
    "composite": true,
    "lib": ["dom", "esnext"],
    "module": "esnext",
    "types": ["../../env/client"]
  }
}
