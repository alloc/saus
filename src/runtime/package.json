{
  "private": true,
  "scripts": {
    "clean": "rimraf dist && git checkout head -- dist",
    "build": "npm run clean && esbuild *.ts **/*.ts --out-extension:.js=.mjs --outdir=dist && tsc --incremental -p .",
    "dev": "npm run clean && tsc --incremental -p . --watch --sourcemap"
  },
  "dependencies": {
    "@cush/relative": "^1.0.0",
    "@utils": "workspace:@saus/utils@*",
    "css-what": "^6.0.1",
    "debug": "^4.3.2",
    "endent": "^2.1.0",
    "etag": "^1.8.1",
    "kleur": "^4.1.4",
    "misty": "^1.6.7",
    "type-fest": "^2.13.0"
  }
}
